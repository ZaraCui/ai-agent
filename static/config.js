// Runtime configuration for the static frontend.
// Replace the values below for your deployment.
// Example backend URL: 'https://travel-agent.onrender.com'

// Set `API_BASE` to the full URL of your deployed backend (no trailing slash).
// If you host the frontend separately (e.g. Vercel) and backend on Render/Heroku/Cloud Run,
// set this to the backend's public URL so the frontend can call the API endpoints.
// Example: const API_BASE = 'https://travel-agent-backend.onrender.com';
// When serving the frontend from the Flask backend itself, you can keep API_BASE as '/'.
const API_BASE = 'https://travel-planning-agent.onrender.com';
